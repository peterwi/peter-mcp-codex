[project]
name = "mcp-base"
version = "0.1.0"
description = "Shared MCP server base for jb-* tools"
requires-python = ">=3.9"
dependencies = [
    "mcp>=0.1.0",
]

[tool.hatch.build.targets.wheel]
packages = ["mcp_base"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
