# MCP Services Configuration
# Reference configuration for available MCP servers in this repository.
# These services can be used with any MCP-compatible client.

[settings]
required_commands = ["uvx", "npx"]
show_services_after_setup = true

[services.sequential_thinking]
description = "Sequential thinking MCP server"
command = "npx -y @modelcontextprotocol/server-sequential-thinking"
env_vars = []
enabled = true

[services.memory]
description = "Memory MCP server"
command = "npx -y @modelcontextprotocol/server-memory"
env_vars = []
enabled = true

[services.playwright]
description = "Playwright MCP server"
command = "npx -y @playwright/mcp@latest"
env_vars = []
enabled = true

[services.pyscn]
description = "Python Code Quality Analyser"
command = "uvx pyscn-mcp"
env_vars = []
enabled = true

[services.kubernetes]
description = "Kubernetes cluster management MCP server"
command = "npx -y kubernetes-mcp-server@latest"
env_vars = []
enabled = true


## Custom MCP servers

[services.cisco]
description = "Cisco NX-OS switch management MCP server"
command = "uvx --from {SCRIPT_DIR}/cisco-mcp cisco-mcp"
env_vars = []
enabled = true

[services.perf]
description = "Linux performance troubleshooting MCP server (USE method)"
command = "node {SCRIPT_DIR}/perf-mcp/dist/index.js"
env_vars = []
enabled = true
